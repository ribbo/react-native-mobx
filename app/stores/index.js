import LayoutsStore from './Layouts';
import Windows from './Windows';

const layoutsStore = new LayoutsStore();
const windowsStore = new Windows();

export default {
  layoutsStore,
  windowsStore,
};
